<h1><%= t('.header', title: @question.test.title) %></h1>
<h4>Current name:
  <%= "'" + @question.body + "'" %>
</h4>

<%= render 'form_edit' %>
<%= link_to 'Back', admin_test_path(@question.test) %>
