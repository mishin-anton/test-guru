<tbody>
  <tr>
      <td><%= question.id %></th>
      <td><%= question.body %></th>
      <td><%= link_to "Delete",
            question_path(question),
            :method => "delete",
            data: { confirm: 'Are you sure?' } %></td>
  </tr>
</tbody>
